{
  "name": "cv",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:pdf": "node scripts/pdf.mjs",
    "build:all": "npm run build && npm run build:pdf",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "playwright": "^1.55.0"
  }
}
